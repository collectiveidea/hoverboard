class User {
  constructor({ id, name, email, website, password, role }) {
    this.id = id
    this.name = name
    this.email = email
    this.website = website
    this.password = password
    this.role = role
  }
}

export { User }
